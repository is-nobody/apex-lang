import os

name = "World"
count = 5
price = 10.5

os.output("Total: {count * 2}")
os.output("Sum: {price + 5}")
os.output("Combined: {count + price}")
os.output("Complex: {(count + 3) * 2}")
os.output("Modulo: {count % 2}")