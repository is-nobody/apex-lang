import os

// Переприсваивание
x_change = 10
os.output("x было {x_change}")
x_change = "строка"
os.output("x стало {x_change}")
x_change = true
os.output("x стало {x_change}")
x_change = none
os.output("x стало {x_change}")