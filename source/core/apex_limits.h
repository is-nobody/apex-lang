#ifndef LIMITS_H
#define LIMITS_H

/* Compile-time resource limits (enforced in parser/codegen; VM assumes they hold). */
#define APEX_MAX_CALL_DEPTH 512
#define APEX_MAX_LOOP_DEPTH 512
#define APEX_MAX_CALL_ARGS  64

#endif
